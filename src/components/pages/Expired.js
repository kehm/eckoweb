import React from 'react';
import strings from '../../strings';

/**
 * Render session expired page
 */
const Expired = () => (
    <div className="w-full m-auto pt-12 mb-60 text-white">
        <h4>{strings.expired}</h4>
    </div>
);

export default Expired;
